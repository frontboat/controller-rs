query FetchController($username: String!, $chainId: String!) {
  controller(username: $username, chainId: $chainId) {
    address
    signers {
      metadata {
        __typename
        ... on PasswordCredentials {
          password {
            publicKey
            encryptedPrivateKey
          }
        }
      }
    }
  }
}
